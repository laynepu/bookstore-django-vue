# PostgreSQL Configuration Example

# Database Credentials
POSTGRES_USER=your_db_user         # PostgreSQL username
POSTGRES_PASSWORD=your_db_password # PostgreSQL password
POSTGRES_DB=your_db_name           # Name of the PostgreSQL database

# Connection Settings
POSTGRES_HOST=0.0.0.0              # Database host (Use "db" if running in Docker Compose)
POSTGRES_PORT=5432                 # Default PostgreSQL port (5432)